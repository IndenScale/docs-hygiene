# Policy Engine

A policy engine evaluates repository documentation against explicit rules that can run locally and in CI.

In Docs Hygiene, the policy engine focuses on documentation structure contracts rather than Markdown syntax.
