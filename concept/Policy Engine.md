# Policy Engine

A policy engine evaluates project documentation against explicit rules that can
run locally and in CI from a selected project root.

In Docs Hygiene, the policy engine focuses on documentation structure contracts rather than Markdown syntax.
