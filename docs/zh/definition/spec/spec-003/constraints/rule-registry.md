---
id: SPEC-003-C-002
status: proposed
---

# C-002 规则注册表

一个有序注册表拥有每个稳定规则标识、能力维度、最低成熟度、适用性证据、默认执行
策略、checker、诊断码、修复方向和例外行为。配置校验、调度、画像评估、解释输出和
生成式规则文档共同消费该注册表。

原有八个激活规则族仍是兼容分组，不是粒度足够的成熟度证据；新增独立的
`governance.topology` 规则族，但不重命名或重排原有 ID。跨维度或跨等级的规则族必须
公开原子注册不变量，同时在迁移期保留公开规则族 ID 和现有诊断码。
