---
id: SPEC-003-C-001
status: proposed
---

# C-001 画像评估

每个维度记录适用性、是否参与总体等级、目标成熟度、检测成熟度和有序证据。只有该等级
及所有更低等级中每个适用不变量都通过时，才检测为达到该等级；执行严重程度不改变该
符合性结果。

因不适用而 inactive 的规则不参与。显式 disabled、跳过或被抑制的检查不能证明不变量，
必须报告为未验证。可选总体等级取所有适用且必需维度的最低检测结果。显式
`notApplicable` 维度不参与计算，但报告必须保留理由。
