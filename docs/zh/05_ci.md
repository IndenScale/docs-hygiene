# CI

Docs Hygiene 设计为既能本地运行，也能作为 CI 门禁运行。

## 本地检查

```bash
cargo run -- check --fail-on-warning
```

如果已经安装二进制：

```bash
docs-hygiene check --fail-on-warning
```

## JSON 输出

当其他工具需要消费诊断结果时，可以使用 JSON。JSON 报告采用带版本号的
LSP 风格诊断形状，但仍然只是 CLI 输出契约。

```bash
docs-hygiene check --format json
```

顶层字段：

- `schemaVersion`
- `summary`
- `diagnostics`

每条诊断包含 `source`、`code`、`severity`、`message`、`path`、`uri`、
`range` 和 `relatedInformation`。

## 测试套件

Rust 测试套件直接覆盖 policy engine：

```bash
cargo test
```

当前测试覆盖必需文件、错误命名、重复编号、断号、长度预算、语言阈值、概念外键、adapter 失败和豁免匹配。

CLI 集成测试覆盖 `--help`、`--version`、`scaffold` 和语言策略 CRUD 命令。

## GitHub Actions

在 Rust 环境准备完成后，GitHub Actions 可以用一个最小步骤运行检查。

```yaml
- name: Check docs hygiene
  run: cargo run -- check --fail-on-warning
```

## 后续 CI 能力

最有价值的下一个输出格式是 SARIF，这样 GitHub 可以在 pull request 上展示诊断结果。
