---
id: DH-GOVERNANCE-EDGE
status: baselined
---

# 治理边

从一个受治理身份指向另一个受治理身份的稳定类型化关系。语义引用和锁定引用共享目标
解析、变更影响、生命周期和拓扑治理能力。Issue 适配器还可以暴露 `addresses`、
`dependsOn` 与 `evidencedBy` 关系。用于导航的 Markdown Link 不是语义依赖。
