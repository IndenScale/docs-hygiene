---
id: SPEC-002-INPUTS
status: baselined
---

# Inputs

Inputs are one project root, one parsed policy, deterministic file metadata and
content facts, plus optional per-rule `auto`, `required`, or `disabled` modes.
