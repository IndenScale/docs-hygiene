---
id: PRD-003-FR-005
status: baselined
---

# Explicit Overrides

Per-rule `disabled` and `required` modes override automatic inference. `auto`
remains the default and never overrides an explicit project decision.
